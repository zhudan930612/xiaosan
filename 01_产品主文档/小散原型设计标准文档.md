# 原型设计标准文档

> 小型工程安全纳管系统 UI 设计规范

---

## 1. 设计原则

- **iOS原生风格**：毛玻璃、大圆角、线性图标
- **清晰层级**：字体大小、字重、色彩区分信息层级
- **呼吸感留白**：20px页面边距，16px卡片间距
- **精致细节**：12px卡片圆角，10px按钮圆角，**头像圆形**
- **线性图标**：使用SVG线性图标（stroke-width: 1.5-2px）
  - **禁止使用emoji**作为图标
  - **禁止使用中文文字**（如[首页][我的]）作为图标占位符
  - **必须使用内联SVG**，确保视觉一致性和可替换性

**应用场景**：
- 备案申请（小程序端）
- 巡查检查（APP端）
- 监管PC端（Web端）：备案审核、工程管理、审批记录
- 管理后台（Web端）：企业中心、运营管理、系统管理

### 1.1 多端原型基线约束（强制）

- 原型设计前，必须先判断需求归属以下哪个端：施工端移动端、监管端移动端、巡检端移动端、监管端 PC 端、大屏 PC 端。
- 一旦归属明确，页面结构、导航方式、信息密度和视觉语言必须沿用该端现有基线，不得把一个端的页面直接改写成另一个端的骨架。
- 施工端移动端：以“项目中心 + 更多服务”为基础，承载备案、整改、核销、安全投诉等施工相关业务。
- 监管端移动端：当前以备案审核为主，优先沿用项目中心、状态切换和工程列表结构。
- 巡检端移动端：当前以巡查、隐患整改跟踪、核销等巡检业务为主，优先沿用巡检统计、待巡查提醒、工程卡片和记录列表结构。
- 监管端 PC 端：当前以备案审核为主，优先沿用左侧分类菜单、顶部筛选区、状态统计卡和表格列表结构。
- 大屏 PC 端：当前同时覆盖备案和巡查，优先沿用地图底座、指标卡、图表分布和动态看板结构。

### 1.2 移动端三类工程 Tab 约束（强制）

- 小型工程、住宅装修、零星作业是移动端页面的默认工程分类主切换。
- 以后凡是移动端原型涉及这三类工程的共用业务场景，默认必须提供“小型工程 / 住宅装修 / 零星作业”三类 `Tab`。
- 如某功能只适用于其中一类或两类工程，必须先在功能设计文档中写明适用范围，原型中才允许隐藏不适用的 `Tab`，并在右侧标注区说明原因。
- 不得把应由三类工程 `Tab` 承载的主切换，擅自改成下拉筛选、弹窗筛选、单列表直出或页面跳转选择。
- 如页面空间不足，优先优化卡片层级、分段展示和状态折叠，不得直接删除三类工程 `Tab`。

---

## 2. 色彩规范

### 2.1 色板速查

| 角色 | 色值 | 使用场景 |
|-----|------|---------|
| 品牌主色 | `#00B578` | 主按钮、选中状态、渐变起始 |
| 品牌辅色 | `#00A86B` | 渐变结束色 |
| 品牌浅绿 | `#E6F7F0` | 浅色背景、标签背景 |
| **状态栏绿** | `#35ad73` | 顶部状态栏渐变起始 |
| **状态栏青** | `#58b8d7` | 顶部状态栏渐变结束、按钮渐变结束 |
| **品牌深绿** | `#1a8f62` | 选中Tab文字、强调色 |
| **按钮渐变绿** | `#27a66d` | 主按钮渐变起始 |
| **按钮渐变青** | `#4fb3d7` | 主按钮渐变结束 |
| 成功/通过 | `#00B578` | 审核通过、正常状态 |
| 警告/待办 | `#FF9500` | 待审核、待处理 |
| 错误/拒绝 | `#FF3B30` | 审核不通过、异常状态 |
| 风险/高危 | `#FF4D4F` | 高风险项目 |
| 处理中 | `#1890FF` | 待受理 |
| 主文字 | `#333333` | 标题、重要内容 |
| 次要文字 | `#666666` | 正文、描述 |
| 辅助文字 | `#999999` | 提示、说明 |
| 页面背景 | `#F5F5F5` | 页面底色 |
| 卡片背景 | `#FFFFFF` | 内容卡片 |

### 2.2 渐变规范

- **导航渐变**：`#00B578` → `#00A86B`（从上到下）
- **状态卡片**：`#00B578` → `#00A86B`（135度对角）
- **顶部状态栏**：`#35ad73` → `#58b8d7`（绿色到青色，从左到右）
- **页面背景**：`#f5faf7` → `#ffffff`（从上到下）
- **选中Tab背景**：`#ffffff` → `#eef9f3` → `#f4fcff`（白色到浅绿到浅蓝）
- **主按钮**：`#27a66d` → `#4fb3d7`（绿色到青色，从左到右）
- **政策卡片背景**：`#ecf8f1` → `#f7fbfd`（浅绿到浅蓝）
- **服务栏背景**：`#f2faf5` → `#f5fbfe`（浅绿到浅蓝）

### 2.3 管理后台色彩规范（Web端）

| 角色 | 色值 | 使用场景 |
|-----|------|---------|
| 顶部/侧边栏背景 | `#1a2b4a` | 顶部导航栏、左侧侧边栏底色 |
| 顶部栏文字 | `#ffffff` | 平台名称、顶部导航文字 |
| 侧边栏文字 | `#bfcbd9` | 未选中菜单文字 |
| 侧边栏选中文字 | `#ffffff` | 选中菜单文字 |
| 侧边栏选中背景 | `#409EFF` | 选中菜单项高亮背景 |
| Web主按钮 | `#409EFF` | 查询、新增、添加等主操作按钮 |
| Web主按钮悬停 | `#66b1ff` | 主按钮悬停状态 |
| 次按钮/重置 | `#ffffff` | 重置、取消等次要按钮 |
| 次按钮边框 | `#dcdfe6` | 次按钮边框色 |
| 文字链接 | `#409EFF` | 编辑、查看等操作链接 |
| 搜索区域背景 | `#f5f7fa` | 搜索表单区域底色 |
| 表格表头背景 | `#f5f7fa` | 数据表格表头底色 |
| 表格边框 | `#ebeef5` | 表格分割线、边框 |
| 表格行悬停 | `#f5f7fa` | 表格行鼠标悬停底色 |
| 页面背景 | `#ffffff` | 内容区域底色 |
| 内容区背景 | `#f0f2f5` | 页面最外层底色（侧边栏与内容区之间） |
| 面包屑文字 | `#606266` | 面包屑导航文字 |
| 面包屑分隔 | `#c0c4cc` | 面包屑分隔符 |
| 标签页背景 | `#ffffff` | Tab标签背景 |
| 标签页选中 | `#409EFF` | Tab选中下划线 |
| 标签页关闭 | `#c0c4cc` | Tab关闭按钮 |
| 表单边框 | `#dcdfe6` | 输入框、选择器边框 |
| 表单聚焦 | `#409EFF` | 输入框聚焦边框 |
| 必填星号 | `#f56c6c` | 表单必填项红色星号 |
| 开关开启 | `#409EFF` | 开关开启状态 |
| 开关关闭 | `#dcdfe6` | 开关关闭状态背景 |
| 树形图标 | `#409EFF` | 树形菜单展开/折叠图标 |
| 危险操作 | `#f56c6c` | 删除、禁用等危险操作按钮/图标 |

---

## 3. 布局与间距

### 3.1 页面结构

```
┌─────────────────────────────────────────┐
│ 9:41              📶  🔋   ← 状态栏 34px │
├─────────────────────────────────────────┤
│ ‹   页面标题          ⚪   ← 导航栏     │
├─────────────────────────────────────────┤
│  标签1  │ 标签2  │ 标签3  │ 标签4       │  ← Tab栏
├─────────────────────────────────────────┤
│  ▎分组标题                              │
│  ┌─────────────────────────────────┐   │
│  │      内容卡片（圆角18px）        │   │
│  └─────────────────────────────────┘   │
├─────────────────────────────────────────┤
│  [辅助按钮]        [主操作按钮]          │  ← 底部按钮
└─────────────────────────────────────────┘
```

**内容区域背景**：
- 多层渐变背景：
  - 第一层：`linear-gradient(180deg, rgba(89, 183, 216, 0.04) 0%, transparent 180px)`（顶部淡青色）
  - 第二层：`linear-gradient(180deg, #f6fbf8 0%, #ffffff 100%)`（浅绿到白色）
- 内边距：16px
- 最小高度：720px

### 3.2 间距规范

| 元素 | 数值 | 元素 | 数值 |
|-----|------|-----|------|
| 页面边距 | 20px | 卡片圆角 | 12px |
| 卡片内边距 | 20px | 按钮圆角 | 10px |
| 卡片间距 | 16px | 按钮高度 | 50px |
| 分组间距 | 32px | 标签栏高度 | 56px |

### 3.3 字体层级

| 层级 | 字号 | 字重 | 使用场景 |
|-----|------|------|---------|
| 大标题 | 28px | 700 | 页面主标题、欢迎语 |
| 导航标题 | 17px | 600 | 导航栏标题 |
| 分组标题 | 17px | 600 | 卡片分组标题 |
| 正文 | 15px | 400 | 主要内容 |
| 辅助文字 | 13px | 400 | 提示、时间 |
| 按钮文字 | 17px | 600 | 所有按钮 |
| 标签文字 | 13px | 500 | 状态标签 |

---

## 4. 组件规范

### 4.1 页面头部结构（状态栏 + 导航栏 + Tab栏）

**整体结构**：
```
┌─────────────────────────────────────────┐
│ 9:41    📶  🔋          ← 状态栏（34px）  │
├─────────────────────────────────────────┤
│ ‹   页面标题          [菜单] ← 导航栏    │
├─────────────────────────────────────────┤
│ 标签1 │ 标签2 │ 标签3 │ 标签4  ← Tab栏   │
└─────────────────────────────────────────┘
```

#### 状态栏（Status Bar）
- **高度**：34px
- **背景**：线性渐变 `linear-gradient(90deg, #35ad73, #58b8d7)`（绿色到青色）
- **内容**：时间（左侧）、状态图标（右侧：信号、WiFi、电池）
- **文字颜色**：白色
- **字体**：12px，字重800

**状态图标样式**：
- 信号图标：4条竖线，高度递增（8px、11px、14px、17px），圆角
- WiFi图标：三层弧线 + 中心圆点
- 电池图标：圆角矩形边框 + 内部填充

#### 导航栏（Header）
- **背景**：线性渐变 `linear-gradient(90deg, #35ad73, #58b8d7)`（与状态栏连续）
- **内边距**：10px 18px 14px
- **高度**：自适应（包含padding）
- **布局**：三栏网格 `44px 1fr 88px`

**元素规范**：
- **返回按钮**：
  - 位置：左侧
  - 尺寸：34px × 34px
  - 图标：左向箭头 `‹`，32px，字重300，白色
  
- **页面标题**：
  - 位置：居中
  - 字号：18px
  - 字重：800
  - 颜色：白色
  - 对齐：居中

- **右侧菜单按钮**：
  - 位置：右侧
  - 尺寸：78px × 36px
  - 样式：圆角胶囊形（border-radius: 999px）
  - 背景：半透明 `rgba(255,255,255,0.08)` + 边框 `rgba(255,255,255,0.22)`
  - 内容：三个圆点 + 分隔线 + 圆环图标

#### Tab栏（Tabs）
- **背景**：`#f1f3f1`（浅灰色）
- **圆角**：顶部圆角 30px（`border-radius: 30px 30px 0 0`）
- **布局**：等分网格（如4列：`repeat(4, 1fr)`）
- **高度**：自适应（padding: 18px 4px 16px）
- **阴影**：内阴影 `inset 0 1px 0 rgba(255,255,255,0.32)`

**Tab项规范**：
- 默认状态：
  - 背景：透明
  - 颜色：`#303631`（深灰）
  - 字号：14px
  - 字重：700
  
- 选中状态：
  - 背景：渐变 `linear-gradient(135deg, #ffffff 0%, #eef9f3 60%, #f4fcff 100%)`
  - 颜色：`#1a8f62`（品牌深绿）
  - 字重：800
  - 圆角：左上角圆角 26px（`border-radius: 26px 0 0 0`）
  - 阴影：`-10px 0 20px rgba(83, 183, 216, 0.08)`

### 4.2 流程状态卡片

- 背景：渐变
- 圆角：12px
- 内边距：20px
- 右上角悬浮标签：灰色背景，绿色文字

### 4.3 标签导航

- 背景：`rgba(255,255,255,0.75)` + `backdrop-filter: blur(20px)`
- 高度：56px
- 默认：`#8E8E93`
- 选中：`#00B578` + 底部绿色指示器

### 4.4 内容卡片

**标准卡片（entry-card）**：
- 背景：`#ffffff`
- 圆角：18px
- 内边距：16px
- 边框：1px solid `#d7e3db`
- 阴影：`0 12px 28px rgba(26, 41, 32, 0.05)`

**主强调卡片（entry-card.primary）**：
- 背景：渐变 `linear-gradient(180deg, #ebf8f1 0%, #ffffff 100%)`
- 边框：2px solid `rgba(34, 166, 109, 0.5)`
- 圆角：18px
- 内边距：16px

**政策说明卡片（policy）**：
- 背景：渐变 `linear-gradient(180deg, #ecf8f1 0%, #f7fbfd 100%)`
- 边框：2px solid `rgba(34, 166, 109, 0.72)`
- 圆角：22px
- 溢出隐藏

**政策卡片头部（policy-head）**：
- 背景：渐变 `linear-gradient(90deg, #35ad73, #58b8d7)`
- 颜色：白色
- 内边距：14px 16px
- 字号：18px，字重800

**服务栏（service-bar）**：
- 背景：渐变 `linear-gradient(90deg, #f2faf5, #f5fbfe)`
- 边框：1px solid `rgba(34, 166, 109, 0.12)`
- 圆角：20px
- 内边距：14px 16px

### 4.4 信息分组卡片

- 分组标题：17px Semibold，`#000000`
- 内容卡片：白色背景，12px圆角，20px内边距
- 表单项：标签左对齐（`#8E8E93`），值右对齐（`#000000`）

### 4.5 按钮

| 类型 | 背景 | 文字 | 边框 | 阴影 |
|-----|------|-----|------|------|
| 主按钮（渐变） | `linear-gradient(90deg, #27a66d, #4fb3d7)` | 白色 | 无 | `0 12px 26px rgba(34, 166, 109, 0.24)` |
| 主按钮（纯色） | `#00B578` | 白色 | 无 | 无 |
| 次按钮 | 白色 | `#00B578` | 1px solid `#00B578` | 无 |
| 虚线边框按钮 | `#fff` | `#00B578` | 1px dashed `rgba(34, 166, 109, 0.65)` | 无 |
| 文字按钮 | 透明 | `#00B578` | 无 | 无 |
| 底部主按钮 | `linear-gradient(90deg, #26a66d, #4fb3d7)` | 白色 | 无 | `0 12px 30px rgba(34, 166, 109, 0.26)` |

**按钮尺寸规范**：
- 圆角：16px（大按钮）、999px（胶囊按钮）
- 高度：50px（标准）、自适应（内容按钮）
- 内边距：14px 16px
- 字号：16px-18px，字重900

### 4.6 表单组件

**单选框**：
- 未选中：空心圆，边框`#CCCCCC`
- 选中：实心圆`#00B578`，内部白色对勾

**复选框**：
- 未选中：方形边框`#CCCCCC`
- 选中：背景`#00B578`，白色对勾

**开关**：
- 关闭：灰色背景，滑块居左
- 开启：绿色`#00B578`，滑块居右

**标签选择器**：
- 默认：白底灰字，边框`#E5E5E5`
- 选中：绿底白字，16px圆角

**搜索栏**：
- 背景：`#F2F2F7`
- 高度：36px
- 圆角：10px

**文件上传**：
- 尺寸：100px × 100px
- 边框：1px dashed `#00B578`
- 圆角：8px

### 4.7 工程卡片

```
┌─────────────────────────────────────┐
│ [正常施工] [监控]                  │
│ 工程名称                            │
│ [定位] 地址信息                     │
│ [高空][用电][拆改][动火]            │
│ 施工时间 2025-11-11至2026-01-11     │
│ ─────────────────────────────────   │
│ 街道 1  社区 1  物业 1  企业 1      │
│ 2隐患   0隐患   0隐患   2隐患       │
└─────────────────────────────────────┘
```

### 4.8 状态标签

| 状态 | 背景色 | 文字色 |
|-----|-------|-------|
| 高风险项目 | `#FFF2F0` | `#FF4D4F` |
| 一般风险 | `#FFFBE6` | `#FAAD14` |
| 正常施工 | `#E6F7F0` | `#00B578` |
| 待整改 | `#FFF7E6` | `#FF9500` |
| 已整改 | `#F5F5F5` | `#999999` |
| 已驳回 | `#FFF2F0` | `#FF4D4F` |
| 待受理 | `#E6F4FF` | `#1890FF` |

### 4.9 时间线

- 轴线：2px宽，`#E5E5E5`
- 已完成：绿色实心圆 + 白色对勾
- 当前：绿色空心圆
- 待处理：灰色空心圆

### 4.10 SVG线性图标规范

**图标风格**：
- 描边宽度：1.5-2px
- 线端样式：round（圆角线端）
- 拐角样式：round（圆角拐角）
- 填充：none（纯描边，无填充）
- 颜色：根据场景使用品牌绿或系统灰

**常用图标尺寸**：
| 场景 | 尺寸 | 描边宽度 |
|-----|------|---------|
| 导航栏返回 | 24px × 24px | 2px |
| 列表项图标 | 20px × 20px | 1.5px |
| 按钮内图标 | 20px × 20px | 1.5px |
| Tab栏图标 | 28px × 28px | 2px |
| 头像/用户 | 圆形 40px | - |

**图标占位符禁止规则**（强制）：
| 禁止类型 | 错误示例 | 正确做法 |
|---------|---------|---------|
| 中文文字占位 | [首页] [我的] [设置] | 使用对应SVG图标 |
| Emoji表情 | ✅ ⚠️ 📊 🔔 | 使用SVG状态图标 |
| 纯文字标签 | AI问答 数据报表 | 图标+文字组合 |

**图标占位符正确做法**：
- 所有功能图标必须使用内联SVG <svg> 标签
- 状态图标（成功/警告/错误）使用带颜色的SVG
- 导航图标使用线性描边SVG（stroke-width: 1.5-2px）
- 图标尺寸遵循规范：Tab栏28px，列表20px，按钮20px

**图标占位符正确示例**：


**图标占位符错误示例**：


---

**图标颜色规范**：
| 场景 | 颜色 | 示例 |
|-----|------|------|
| 主操作 | `#00B578` | 返回箭头、添加按钮 |
| 次要/默认 | `#8E8E93` | 辅助说明图标 |
| 选中状态 | `#00B578` | Tab栏选中图标 |
| 不可点击 | `#C7C7CC` | 禁用状态图标 |

**常用图标类型**：
- 返回：左向箭头
- 添加：加号（+）
- 搜索：放大镜
- 定位：定位标记
- 文档：文件轮廓
- 用户：人像轮廓
- 相机：相机轮廓
- 设置：齿轮轮廓
- 更多：三个点/右向箭头

---

## 5. 移动端页面类型模板

| 类型 | 适用场景 | 结构 |
|-----|---------|------|
| 表单填写页 | 备案申请、信息录入 | 导航+状态卡片+标签导航+分组表单+底部按钮 |
| 信息展示页 | 详情查看、审核结果 | 导航+状态卡片+分组信息+时间线 |
| 列表浏览页 | 工程列表、任务列表 | 导航+筛选标签+卡片列表 |
| 巡查录入页 | 巡查检查、隐患上报 | 导航+工程信息+检查项+底部按钮 |
| 人员选择页 | 选择工人、巡查员 | 导航+搜索栏+人员列表+底部按钮 |

---

## 6. 监管PC端设计规范（Web端）

### 6.1 整体布局架构

```
┌─────────────────────────────────────────────────────────────┐
│ 青浦区小型工程综合管理平台      政务平台   2026/06/11 测试 ▼  │ ← 顶部栏 56px
├──────────┬──────────────────────────────────────────────────┤
│          │  工程名称  工程地址  所属街道社区  [查询][重置]     │ ← 搜索区
│  小型工程  ├──────────────────────────────────────────────────┤
│  住宅装修  │  [待填报54] [待审核5] [待确定1] [已驳回54] [已通过119]│ ← 状态统计卡
│  零星作业  ├──────────────────────────────────────────────────┤
│  备案回收站│  序号 │ 工程名称 │ 街道社区 │ 地址 │ 面积 │ 造价 │ 备案时间 │ 操作 │
│     6    │  ─────────────────────────────────────────────────  │ ← 数据表格
│          │                                                 │
│  ← 侧边栏 │              内容区域（白色背景）                 │
│  200px   │                                                 │
│          │                                    共5条 20条/页  │ ← 分页
└──────────┴──────────────────────────────────────────────────┘
```

**布局规则**：
- **顶部导航栏**：固定高度 `56px`，左侧平台名称，右侧"政务平台"标签 + 日期时间 + 用户头像下拉
- **左侧侧边栏**：固定宽度 `200px`，可折叠收起，菜单项带图标和角标
- **内容区域**：自适应宽度，`padding: 20px 24px`，背景色 `#ffffff`
- **滚动规则**：内容区域独立滚动，顶部栏和侧边栏固定不动

### 6.2 色彩规范

监管PC端沿用品牌绿色系，与小程序端保持一致：

| 角色 | 色值 | 使用场景 |
|-----|------|---------|
| 顶部栏背景 | `#3CB17F` | 顶部导航栏底色（品牌绿） |
| 顶部栏文字 | `#ffffff` | 平台名称、用户信息 |
| 侧边栏背景 | `#ffffff` | 左侧菜单底色 |
| 侧边栏文字 | `#333333` | 未选中菜单文字 |
| 侧边栏选中背景 | `#E6F7F0` | 选中菜单项背景（浅绿） |
| 侧边栏选中文字 | `#00B578` | 选中菜单文字（品牌绿） |
| 侧边栏图标 | `#999999` | 未选中图标 |
| 侧边栏选中图标 | `#00B578` | 选中图标 |
| 侧边栏角标 | `#FF4D4F` | 角标数字背景 |
| 搜索区域背景 | `#f5f7fa` | 搜索表单区域底色 |
| 查询按钮 | `#3CB17F` | 查询按钮背景 |
| 查询按钮悬停 | `#35A070` | 查询按钮悬停 |
| 重置按钮 | `#ffffff` | 重置按钮背景 |
| 重置按钮边框 | `#dcdfe6` | 重置按钮边框 |
| 主操作按钮 | `#3CB17F` | 通过、确认等主操作 |
| 次操作按钮 | `#FF9500` | 转派、待确定等次操作 |
| 危险操作按钮 | `#FF4D4F` | 驳回、删除等危险操作 |
| 文字链接 | `#3CB17F` | 备案审核、查看等操作链接 |
| 表格表头背景 | `#f5f7fa` | 数据表格表头底色 |
| 表格边框 | `#ebeef5` | 表格分割线、边框 |
| 表格行悬停 | `#f5f7fa` | 表格行鼠标悬停底色 |
| 页面背景 | `#ffffff` | 内容区域底色 |
| 内容区背景 | `#f0f2f5` | 页面最外层底色 |
| 表单边框 | `#dcdfe6` | 输入框、选择器边框 |
| 表单聚焦 | `#3CB17F` | 输入框聚焦边框 |
| 弹窗遮罩 | `rgba(0,0,0,0.5)` | 弹窗背景遮罩 |
| 弹窗标题背景 | `#3CB17F` | 弹窗标题栏背景（品牌绿） |
| 状态标签-待审核 | `#FFF7E6` | 待审核标签背景 |
| 状态标签-待审核文字 | `#FF9500` | 待审核标签文字 |
| 状态标签-已通过 | `#E6F7F0` | 已通过标签背景 |
| 状态标签-已通过文字 | `#00B578` | 已通过标签文字 |
| 状态标签-已驳回 | `#FFF2F0` | 已驳回标签背景 |
| 状态标签-已驳回文字 | `#FF4D4F` | 已驳回标签文字 |

### 6.3 顶部导航栏

- **背景**：`#3CB17F`（品牌绿色）
- **高度**：56px
- **左侧**：平台名称"青浦区小型工程综合管理平台"，白色，18px，字重600
- **右侧**：
  - "政务平台"标签：带建筑图标，白色
  - 日期时间：白色，14px，如"2026/06/11 13:33:21"
  - 分隔线：1px `rgba(255,255,255,0.3)`
  - 用户头像：圆形，32px，默认灰色头像
  - 用户名：白色，14px，如"测试"
  - 下拉箭头：白色，12px
- **阴影**：无

### 6.4 左侧侧边栏

- **背景**：`#ffffff`（白色）
- **宽度**：200px（展开），64px（折叠）
- **边框**：右侧 1px `#e8e8e8`
- **菜单项**：
  - 高度：50px
  - 内边距：0 20px
  - 图标：左侧，18px，与文字间距12px
  - 文字：15px，默认色 `#333333`
  - 选中状态：文字 `#00B578`，背景 `#E6F7F0`，左侧4px绿色竖线
  - 悬停状态：背景 `#f5f5f5`
- **角标**：
  - 位置：菜单项右侧
  - 背景：`#FF4D4F`（红色）
  - 文字：白色，12px
  - 形状：圆形，最小宽度20px，高度20px，内边距0 6px
- **底部折叠按钮**：居中，点击收起/展开侧边栏

### 6.5 搜索区域

- **背景**：`#f5f7fa`
- **圆角**：4px
- **内边距**：16px 20px
- **布局**：表单元素横向排列，Flex布局
- **表单元素间距**：16px
- **元素规范**：
  - 输入框：高度36px，边框1px `#dcdfe6`，圆角4px，内边距0 12px，占位符灰色
  - 选择器：同输入框高度，带下拉箭头
  - **查询按钮**：高度36px，背景 `#3CB17F`，文字白色，圆角4px，左侧放大镜图标
  - **重置按钮**：高度36px，背景白色，边框1px `#dcdfe6`，文字 `#606266`，圆角4px，左侧刷新图标
- **行距**：多行搜索时行间距12px

### 6.6 状态统计卡片

- **布局**：横向排列，Flex布局，等分或自适应
- **卡片样式**：
  - 背景：`#ffffff`
  - 边框：1px `#e8e8e8`
  - 圆角：8px
  - 内边距：16px 20px
  - 阴影：`0 2px 8px rgba(0,0,0,0.04)`
- **卡片内容**：
  - 左侧图标：40px × 40px，圆角8px，带背景色
    - 待填报：浅蓝背景 `#E6F4FF`，蓝色图标 `#1890FF`
    - 待审核：浅橙背景 `#FFF7E6`，橙色图标 `#FF9500`
    - 待确定：浅青背景 `#E6FFFB`，青色图标 `#13C2C2`
    - 已驳回：浅红背景 `#FFF2F0`，红色图标 `#FF4D4F`
    - 已通过：浅绿背景 `#E6F7F0`，绿色图标 `#00B578`
  - 右侧文字：
    - 状态名称：14px，`#666666`
    - 数量：28px，字重700，对应状态色
- **间距**：卡片间距16px

### 6.7 数据表格

- **表格标题栏**：
  - 左侧：标题文字，16px，字重600，`#303133`
  - 右侧：操作按钮（新增、导出等）
  - 高度：48px
- **表头**：
  - 背景：`#f5f7fa`
  - 文字：14px，字重600，`#909399`
  - 行高：40px
  - 边框：底部1px `#ebeef5`
  - 排序图标：默认灰色，选中蓝色
- **表格行**：
  - 文字：14px，`#606266`
  - 行高：48px-56px（根据内容自适应）
  - 悬停：背景 `#f5f7fa`
  - 边框：底部1px `#ebeef5`
- **操作列**：
  - 文字链接：14px，`#3CB17F`
  - 多个操作间用分隔符 `|` 隔开，间距8px
  - 危险操作（删除）：`#FF4D4F`
- **空状态**：表格居中显示"暂无数据" + 空状态图标

### 6.8 分页器

- **位置**：表格下方右侧
- **布局**：共N条 + 每页条数选择器 + 上一页/页码/下一页 + 前往N页
- **高度**：48px
- **文字**：13px，`#606266`
- **页码按钮**：
  - 默认：背景白色，边框1px `#dcdfe6`，圆角2px
  - 选中：背景 `#3CB17F`，文字白色，边框 `#3CB17F`
  - 悬停：文字 `#3CB17F`，边框 `#3CB17F`
- **每页条数选择器**：下拉选择 10/20/50/100 条
- **快捷跳转**：输入页码 + "前往" 按钮

### 6.9 审核弹窗

#### 弹窗整体结构

```
┌─────────────────────────────────────────────────────────┐
│  [待审核]      备案信息审核                    [审批记录] │ ← 标题栏
├─────────────────────────────────────────────────────────┤
│  基础信息 │ 工程信息 │ 安全信息 │ 保单信息              │ ← Tab栏
├─────────────────────────────────────────────────────────┤
│  项目信息                                               │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 工程名称      11                                 │   │
│  │ 工程类型      (五)既有建筑修缮工程                │   │ ← 表单区域
│  │ 作业场所      体育场场所                          │   │
│  │ ...                                               │   │
│  └─────────────────────────────────────────────────┘   │
│  建设单位                                               │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 建设单位身份    产权人                            │   │
│  └─────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│            [驳回]    [转派]    [通过]                   │ ← 底部操作按钮
└─────────────────────────────────────────────────────────┘
```

#### 弹窗标题栏

- **背景**：`#3CB17F`（品牌绿）
- **高度**：48px
- **左侧**：
  - 状态标签：圆角胶囊形（border-radius: 999px）
    - 待审核：背景 `#FF9500`，文字白色，内边距 2px 12px，字号12px
    - 已通过：背景 `#00B578`，文字白色
    - 已驳回：背景 `#FF4D4F`，文字白色
- **中间**：弹窗标题，白色，16px，字重600，居中
- **右侧**：
  - "审批记录"按钮：白色边框按钮，圆角4px，文字白色，带文档图标，内边距 4px 12px
  - 关闭按钮：× 图标，白色，20px，点击关闭弹窗

#### 弹窗内容区

- **背景**：`#ffffff`
- **内边距**：20px 24px
- **最大高度**：视口高度80%，超出滚动
- **宽度**：800px-900px（根据内容调整）

#### Tab栏

- **位置**：标题栏下方
- **高度**：44px
- **背景**：白色，底部1px边框 `#e8e8e8`
- **Tab项**：
  - 默认：文字 `#666666`，14px，内边距 10px 20px
  - 选中：文字 `#3CB17F`，底部2px `#3CB17F` 下划线，字重600
  - 悬停：文字 `#3CB17F`
- **Tab内容区**：内边距 16px 0

#### 表单展示样式

- **分组标题**：
  - 文字：16px，字重600，`#333333`
  - 间距：上方20px，下方12px
- **表单项**：
  - 布局：标签左对齐，值左对齐
  - 标签：14px，`#666666`，宽度120px
  - 值：14px，`#333333`
  - 行高：40px
  - 边框：底部1px `#f0f0f0`（分隔线）
- **多行内容**：
  - 如"主要施工内容"：标签上方，值下方，自动换行
  - 最小高度：40px，内边距 8px 0

#### 底部操作按钮

- **位置**：弹窗底部，固定
- **背景**：白色，顶部1px边框 `#e8e8e8`
- **内边距**：16px 24px
- **按钮排列**：右对齐，间距12px
- **按钮样式**：
  - **驳回按钮**：
    - 背景：`#FF4D4F`
    - 文字：白色，14px
    - 圆角：4px
    - 高度：36px
    - 内边距：0 24px
  - **转派按钮**：
    - 背景：`#FF9500`
    - 文字：白色，14px
    - 圆角：4px
    - 高度：36px
    - 内边距：0 24px
  - **通过按钮**：
    - 背景：`#3CB17F`
    - 文字：白色，14px
    - 圆角：4px
    - 高度：36px
    - 内边距：0 24px

### 6.10 审批记录弹窗

#### 弹窗整体结构

```
┌─────────────────────────────────────────────────────────┐
│              审批记录                          [×]       │ ← 标题栏
├─────────────────────────────────────────────────────────┤
│  ○  待施工单位重新填报                                  │
│  │                                                      │
│  ○  04-23 15:41                                         │
│  │   AI审核驳回                                          │
│  │   ─────────────────────────────────────────           │
│  │   驳回原因                                            │
│  │   1.施工单位资质证书：图片不清晰...                    │
│  │   2.承发包合同：图片不清晰...                          │
│  │   ...                                                 │
│  │   由AI员工（小华）提供                                 │
│  │                                                      │
│  ○  04-23 15:41                                         │
│  │   提交备案申请                                         │
│  │   提交人    施工用户_13211111111    联系电话 132****111│
│  ...                                                    │
└─────────────────────────────────────────────────────────┘
```

#### 弹窗标题栏

- **背景**：`#3CB17F`（品牌绿）
- **高度**：48px
- **中间**："审批记录"，白色，16px，字重600，居中
- **右侧**：关闭按钮 ×，白色，20px

#### 时间线样式

- **轴线**：1px宽，`#e8e8e8`，纵向贯穿
- **时间节点**：
  - 圆点：12px，空心圆，边框2px `#3CB17F`
  - 或实心圆，背景 `#3CB17F`
  - 圆点位置：轴线左侧，水平居中于轴线
- **时间标签**：
  - 格式："MM-DD HH:mm"，如"04-23 15:41"
  - 文字：14px，`#666666`
  - 位置：节点上方或节点右侧
- **记录卡片**：
  - 背景：`#fafafa`
  - 圆角：4px
  - 内边距：12px 16px
  - 边框：1px `#f0f0f0`
  - 间距：与上下节点间距16px
- **记录内容**：
  - 操作类型：14px，字重600，`#333333`
    - AI审核驳回、提交备案申请、人工审核通过等
  - 驳回原因：
    - 标题："驳回原因"，14px，字重600，`#333333`
    - 内容：13px，`#FF4D4F`（红色），逐条列出
    - 编号：1. 2. 3.
  - 提交信息：
    - 标签：13px，`#999999`
    - 值：13px，`#333333`
    - 布局：标签-值横向排列，间距8px
  - 操作人：
    - 文字：13px，`#999999`
    - 格式："由XXX提供"或"提交人：XXX"
- **时间线首项**：
  - 可能无时间戳，仅显示状态文本，如"待施工单位重新填报"
  - 文字：14px，`#666666`

### 6.11 监管PC端页面类型模板

| 类型 | 适用场景 | 结构 |
|-----|---------|------|
| 列表管理页 | 工程列表、备案列表 | 顶部栏+侧边栏+搜索区+状态统计卡+表格+分页 |
| 审核弹窗页 | 备案审核、信息审核 | 弹窗标题+状态标签+Tab栏+分组表单+底部操作按钮 |
| 审批记录页 | 查看审批历史 | 弹窗标题+时间线+记录卡片 |
| 详情查看页 | 工程详情、保单详情 | 弹窗标题+Tab栏+分组信息展示 |

---

## 7. 管理后台设计规范（Web端）

### 7.1 整体布局架构

```
┌─────────────────────────────────────────────────────────────┐
│ 小型工程全域全量安全监管平台    企业中心  运营管理  系统管理    │ ← 顶部导航栏 56px
│                                      超级管理员 ▼           │
├──────────┬──────────────────────────────────────────────────┤
│          │  企业中心  >  施工单位                            │ ← 面包屑 40px
│  物业单位  ├──────────────────────────────────────────────────┤
│  监管单位  │  施工单位 ×  │  楼栋管理  │  导航管理           │ ← 标签页Tab
│  施工工人  ├──────────────────────────────────────────────────┤
│          │                                                 │
│  楼栋管理  │              内容区域（白色背景）                 │
│  小程序配置 │                                                 │
│  用户管理  │                                                 │
│          │                                                 │
│  ← 侧边栏 │                                                 │
│  200px   │                                                 │
│          │                                                 │
└──────────┴──────────────────────────────────────────────────┘
```

**布局规则**：
- **顶部导航栏**：固定高度 `56px`，背景色 `#1a2b4a`，左侧平台Logo+名称，中间主导航，右侧用户头像下拉
- **左侧侧边栏**：固定宽度 `200px`，背景色 `#1a2b4a`，可折叠收起，菜单项带图标
- **内容区域**：自适应宽度，`padding: 20px 24px`，背景色 `#ffffff`
- **外层背景**：侧边栏与内容区整体背景 `#f0f2f5`
- **滚动规则**：内容区域独立滚动，顶部栏和侧边栏固定不动

### 7.2 布局尺寸规范

| 元素 | 尺寸 | 说明 |
|-----|------|------|
| 顶部导航栏高度 | 56px | 固定 |
| 侧边栏宽度 | 200px | 展开状态，可折叠至 64px |
| 面包屑区域高度 | 40px | 内含内边距 |
| 内容区内边距 | 20px 24px | 上下20px，左右24px |
| 搜索区域内边距 | 16px 20px | 浅灰背景卡片 |
| 表格标题区高度 | 48px | 含操作按钮 |
| 表格行高 | 48px | 标准行高 |
| 分页区域高度 | 48px | 右下对齐 |
| 标签页高度 | 40px | 可关闭标签 |

### 7.3 顶部导航栏

- **背景**：`#1a2b4a`（深蓝/藏青色）
- **高度**：56px
- **左侧**：平台Logo + 名称"小型工程全域全量安全监管平台"，白色，16px，字重600
- **中间**：主导航菜单（企业中心、运营管理、系统管理），白色，14px
  - 默认状态：白色 60%透明度
  - 悬停状态：白色 100%
  - 选中状态：白色 100%，底部2px白色下划线
- **右侧**：通知图标 + 头像（圆形32px）+ "超级管理员" + 下拉箭头
- **阴影**：`0 1px 4px rgba(0,0,0,0.08)`

### 7.4 左侧侧边栏

- **背景**：`#1a2b4a`
- **宽度**：200px（展开），64px（折叠）
- **菜单项**：
  - 高度：44px
  - 内边距：0 20px
  - 图标：左侧，16px，与文字间距8px
  - 文字：14px，默认色 `#bfcbd9`
  - 选中状态：文字 `#ffffff`，背景 `#409EFF`，左侧4px蓝色竖线
  - 悬停状态：文字 `#ffffff`
- **子菜单**：缩进20px，文字13px
- **底部折叠按钮**：居中，点击收起/展开侧边栏

### 7.5 面包屑导航

- **高度**：40px
- **内边距**：0 24px
- **背景**：透明（内容区白色背景上）
- **文字**：14px，`#606266`
- **分隔符**：`>`，`#c0c4cc`
- **当前页**：不显示链接样式
- **可点击项**：悬停变 `#409EFF`，带下划线

### 7.6 多标签页（Tab）

- **位置**：面包屑下方，内容区顶部
- **高度**：40px
- **背景**：白色，底部1px边框 `#e4e7ed`
- **标签项**：
  - 默认：文字 `#606266`，14px，内边距 8px 16px
  - 选中：文字 `#409EFF`，底部2px `#409EFF` 下划线
  - 悬停：文字 `#409EFF`
  - 关闭按钮：右侧 × 图标，默认 `#c0c4cc`，悬停 `#f56c6c` + 红色圆形背景
- **首页标签**：固定不可关闭
- **右键菜单**：刷新、关闭、关闭其他、关闭全部

### 7.7 搜索区域

- **背景**：`#f5f7fa`
- **圆角**：4px
- **内边距**：16px 20px
- **布局**：表单元素横向排列，Flex布局
- **表单元素间距**：16px
- **元素规范**：
  - 输入框：高度36px，边框1px `#dcdfe6`，圆角4px，内边距0 12px
  - 选择器：同输入框高度，带下拉箭头
  - 日期选择器：同输入框高度，带日历图标
  - **查询按钮**：高度36px，背景 `#409EFF`，文字白色，圆角4px，左侧放大镜图标
  - **重置按钮**：高度36px，背景白色，边框1px `#dcdfe6`，文字 `#606266`，圆角4px，左侧刷新图标
- **行距**：多行搜索时行间距12px
- **展开/收起**：条件过多时支持展开/收起，默认显示第一行

### 7.8 数据表格

- **表格标题栏**：
  - 左侧：标题文字，16px，字重600，`#303133`
  - 右侧：新增按钮（蓝色，+图标）+ 分页大小选择器 + 导出按钮
  - 高度：48px
- **表头**：
  - 背景：`#f5f7fa`
  - 文字：14px，字重600，`#909399`
  - 行高：40px
  - 边框：底部1px `#ebeef5`
  - 排序图标：默认灰色，选中蓝色
- **表格行**：
  - 文字：14px，`#606266`
  - 行高：48px
  - 斑马纹：偶数行背景 `#fafafa`（可选）
  - 悬停：背景 `#f5f7fa`
  - 边框：底部1px `#ebeef5`
- **单元格**：
  - 内边距：12px 16px
  - 文字溢出：省略号，可设置 tooltip
- **操作列**：
  - 文字链接：14px，`#409EFF`
  - 多个操作间用分隔符 `|` 隔开，间距8px
  - 危险操作（删除）：`#f56c6c`
- **空状态**：表格居中显示"暂无数据" + 空状态图标
- **加载状态**：表格内显示骨架屏或转圈加载

### 7.9 分页器

- **位置**：表格下方右侧
- **布局**：共N条 + 每页条数选择器 + 上一页/页码/下一页 + 前往N页
- **高度**：48px
- **文字**：13px，`#606266`
- **页码按钮**：
  - 默认：背景白色，边框1px `#dcdfe6`，圆角2px
  - 选中：背景 `#409EFF`，文字白色，边框 `#409EFF`
  - 悬停：文字 `#409EFF`，边框 `#409EFF`
- **每页条数选择器**：下拉选择 10/20/50/100 条
- **快捷跳转**：输入页码 + "前往" 按钮

### 7.10 表单组件

**输入框**：
- 高度：36px（标准），48px（大）
- 边框：1px `#dcdfe6`，圆角4px
- 聚焦：边框 `#409EFF`，外发光 `0 0 0 2px rgba(64,158,255,0.2)`
- 禁用：背景 `#f5f7fa`，文字 `#c0c4cc`
- 占位符：`#c0c4cc`，14px

**选择器（下拉框）**：
- 同输入框尺寸
- 下拉箭头：右侧，`#c0c4cc`
- 下拉面板：白色背景，阴影 `0 2px 12px rgba(0,0,0,0.1)`，圆角4px

**单选框/复选框**：
- 未选中：边框2px `#dcdfe6`，尺寸14px
- 选中：背景 `#409EFF`，边框 `#409EFF`，内部白色对勾/圆点

**开关**：
- 尺寸：宽度36px，高度20px
- 关闭：背景 `#dcdfe6`，滑块白色
- 开启：背景 `#409EFF`，滑块白色
- 滑块：圆形，直径16px

**日期选择器**：
- 同输入框尺寸
- 日历图标：右侧，`#c0c4cc`
- 面板：白色背景，圆角4px，阴影

**文本域**：
- 最小高度：80px
- 边框/聚焦/禁用同输入框
- 右下角显示字数统计

**表单布局**：
- **标签右对齐**：标签宽度固定（如120px），右对齐，文字14px `#606266`
- **必填项**：标签前红色星号 `*`，间距4px
- **表单项间距**：底部20px
- **帮助文字**：标签下方或输入框下方，12px，`#909399`
- **错误提示**：输入框下方，12px，`#f56c6c`

### 7.11 按钮规范

| 类型 | 背景 | 文字 | 边框 | 使用场景 |
|-----|------|-----|------|---------|
| 主按钮 | `#409EFF` | 白色 | 无 | 查询、新增、保存、确认 |
| 主按钮悬停 | `#66b1ff` | 白色 | 无 | - |
| 次按钮 | `#ffffff` | `#606266` | 1px `#dcdfe6` | 重置、取消、返回 |
| 次按钮悬停 | `#409EFF` | `#ffffff` | 1px `#409EFF` | - |
| 文字按钮 | 透明 | `#409EFF` | 无 | 编辑、查看、详情 |
| 危险按钮 | `#f56c6c` | 白色 | 无 | 删除、禁用 |
| 图标按钮 | `#409EFF` | 白色 | 无 | +、设置、导出（带图标） |

**按钮尺寸**：
- 大：高度40px，内边距12px 24px，圆角4px
- 中（默认）：高度36px，内边距9px 20px，圆角4px
- 小：高度32px，内边距7px 16px，圆角4px

### 7.12 树形菜单/列表

- **节点高度**：40px
- **缩进**：每级18px
- **展开/折叠图标**：右侧三角形/箭头，`#409EFF`，12px
- **节点文字**：14px，`#606266`
- **选中节点**：背景 `#f5f7fa`，文字 `#409EFF`
- **悬停节点**：背景 `#f5f7fa`
- **操作图标**：节点右侧，+（添加）、编辑、删除（垃圾桶），默认灰色，悬停显示
- **节点间距**：2px

### 7.13 左右分栏布局（配置页）

- **左侧列表区**：宽度40%，最小宽300px
  - 标题栏："XX列表"，16px字重600
  - 列表：树形或普通列表
  - 操作按钮：添加XX（蓝色按钮）
- **右侧详情区**：宽度60%，弹性填充
  - 标题栏："XX内容"，16px字重600
  - 表单：标签右对齐，表单项纵向排列
  - 底部分割线分隔不同区块
- **分隔线**：中间1px `#e4e7ed`，不可拖拽
- **两栏间距**：0px（共用边框）

### 7.14 管理后台页面类型模板

| 类型 | 适用场景 | 结构 |
|-----|---------|------|
| 列表管理页 | 施工单位、楼栋、用户等 | 面包屑+标签页+搜索区+表格+分页 |
| 表单编辑页 | 新增/编辑信息 | 面包屑+标签页+表单+底部操作按钮 |
| 配置管理页 | 导航管理、菜单配置 | 面包屑+标签页+左右分栏（列表+表单） |
| 详情查看页 | 查看详情、审批 | 面包屑+标签页+信息卡片+操作按钮 |
| 系统日志页 | 日志查询、操作记录 | 面包屑+标签页+搜索区+表格+分页 |

---

## 8. 原型输出规范

### 8.1 标注规范（强制）

**标注样式**：
- 背景：`#FFFBE6`（浅黄色）
- 边框：1px dashed `#FFC53D`
- 圆角：4px
- 标题：📌 需求说明，14px 加粗
- 正文：13px 常规

**标注内容性质**：**需求逻辑规则说明**，不是交互说明

**标注内容应包含**：
| 类型 | 说明 | 示例 |
|------|------|------|
| **数据规则** | 计算口径、数据来源、更新频率 | 巡查覆盖率 = 已巡查工程数 / 总工程数 |
| **业务规则** | 状态流转、权限判断、异常处理 | 隐患整改期限：一般3天，较大7天 |
| **字段规则** | 字段定义、校验逻辑、枚举值 | 风险等级：高风险/一般风险/低风险 |
| **需求说明** | 功能点描述、逻辑说明 | 本周指自然周（周一至周日） |

**标注内容不应包含**：
- ❌ 不写交互操作（点击哪里、怎么跳转）
- ❌ 不写页面流程（进入哪个页面）
- ❌ 不写显而易见的操作说明

**标注内容示例**：
```markdown
📌 巡查覆盖率计算规则
- 分子：本周有巡查记录的在建工程数
- 分母：当前在建工程总数（状态=正常施工|现场整改|停工整改）
- 计算公式：分子 / 分母 × 100%
- 数据来源：巡查记录表、工程状态表
- 更新频率：每日凌晨 2:00 自动更新
- 口径说明：暂按"有巡查即覆盖"，后续按频次要求

📌 隐患等级判定规则
- 较大隐患：需停工整改的隐患（如高处坠落风险）
- 一般隐患：现场整改或不限时整改的隐患
- 判定依据：标准化隐患清单 V2.0

📌 整改状态流转
- 待整改 → 整改中（提交整改记录）
- 整改中 → 已整改（审核通过）
- 整改中 → 待整改（审核不通过，退回）
```

**大屏/管理后台标注位置（强制抽屉式）**：
- 使用抽屉式标注，默认收起
- 固定在页面右侧，垂直居中
- 点击"标注"按钮展开/收起

### 8.2 HTML原型规范

**高度自适应**（强制）：
- `.phone-frame` 使用 `height: auto`，禁止固定高度
- `.phone-screen` 使用 `height: auto`
- 禁止 `<div style="height: 100px;">` 占位元素

**定位约束**（强制）：
- 底部按钮使用相对定位或 flex 布局
- 禁止使用 `position: fixed`

**弹窗默认隐藏**（强制）：
- 默认 `display: none`
- 通过 JS 控制显示/隐藏

**可编辑原则**：
- 文案使用 `contenteditable="true"`
- hover 显示绿色边框，focus 显示绿色描边

**移动端原型布局标准（强制）**：
- 整体布局：左侧手机原型区 + 右侧需求说明区域，左右并排
- 主容器 `.main-container` 使用 `display: flex; flex-direction: row; flex-wrap: nowrap;`
- 主容器设置 `min-width: fit-content; margin: 0 auto;`，禁止自动换行且整体居中
- body 设置 `overflow-x: auto;`，当视口不够宽时支持水平滚动
- 左侧 `.prototype-area` 固定宽度（手机框架宽度），`flex-shrink: 0`
- 右侧 `.annotation-area` 弹性宽度，`min-width: 400px; max-width: 600px`
- 两区域间距：`gap: 24px`

**页面导航标准（强制）**：
- 使用浮动导航栏，默认隐藏，不遮挡原型内容
- 快捷键 `F8` 控制导航显示/隐藏
- 导航包含：页面指示器 + 各页面快捷跳转按钮
- 导航位置固定在底部居中

**需求说明区域标准（强制）**：
- 每个页面必须有独立的需求说明区域，跟随页面切换同步显示/隐藏
- 说明区域样式：背景 `#FFFBE6`，边框 `2px dashed #FFC53D`，圆角 `12px`
- 说明区域必须系统性覆盖5类规则：
  1. 页面状态规则
  2. 数据规则
  3. 字段规则
  4. 交互分支规则
  5. 边界情况规则
- 每个页面切换时，右侧说明区域同步切换对应内容
- 标题格式：`页面N：页面名称 — 需求说明`

---

## 9. 附录：速查表

### 9.1 移动端色板

```
品牌主色     #00B578    品牌辅色     #00A86B
成功/正常    #00B578    警告/待办    #FF9500
错误/驳回    #FF3B30    风险/高危    #FF4D4F
处理中       #1890FF    一般风险     #FAAD14
主文字       #333333    次要文字     #666666
辅助文字     #999999    页面背景     #F5F5F5
卡片背景     #FFFFFF    分割线       #E5E5E5

【渐变色值】
状态栏渐变   #35ad73 → #58b8d7（绿→青，横向）
按钮渐变     #27a66d → #4fb3d7（绿→青，横向）
Tab选中背景  #ffffff → #eef9f3 → #f4fcff（白→绿→蓝）
政策卡片     #ecf8f1 → #f7fbfd（绿→蓝）
服务栏       #f2faf5 → #f5fbfe（绿→蓝）
```

### 9.2 监管PC端色板

```
顶部栏背景   #3CB17F    侧边栏背景   #ffffff
侧边栏选中   #E6F7F0    侧边栏选中字 #00B578
查询按钮     #3CB17F    文字链接     #3CB17F
主操作按钮   #3CB17F    次操作按钮   #FF9500
危险操作     #FF4D4F    搜索区域     #f5f7fa
表格表头     #f5f7fa    表格边框     #ebeef5
页面背景     #ffffff    外层背景     #f0f2f5
弹窗标题     #3CB17F    表单边框     #dcdfe6
```

### 9.3 管理后台色板

```
顶部/侧边栏  #1a2b4a    侧边栏文字   #bfcbd9
Web主按钮    #409EFF    主按钮悬停   #66b1ff
次按钮边框   #dcdfe6    文字链接     #409EFF
搜索区域     #f5f7fa    表格表头     #f5f7fa
表格边框     #ebeef5    页面背景     #ffffff
外层背景     #f0f2f5    面包屑文字   #606266
标签页选中   #409EFF    表单边框     #dcdfe6
表单聚焦     #409EFF    必填星号     #f56c6c
开关开启     #409EFF    危险操作     #f56c6c
```

### 9.4 移动端尺寸

```
页面边距：20px      卡片内边距：20px
卡片间距：16px      卡片圆角：12px
按钮圆角：10px      按钮高度：50px
标签栏高：56px      导航栏高：44px
```

### 9.5 监管PC端尺寸

```
顶部栏高：56px      侧边栏宽：200px
搜索区内边：16px 20px  表格行高：48px
分页高度：48px      弹窗宽：800-900px
状态卡图标：40px    操作按钮高：36px
```

### 9.6 管理后台尺寸

```
顶部栏高：56px      侧边栏宽：200px
面包屑高：40px      内容边距：20px 24px
搜索区内边：16px 20px  表格行高：48px
分页高度：48px      标签页高：40px
输入框高：36px      按钮高（中）：36px
```

### 9.7 标注样式

```
标注背景     #FFFBE6    标注边框     #FFC53D（虚线）
标注圆角     4px        标注内边距   12px
```

---

## 10. 更新记录

| 日期 | 版本 | 更新内容 |
|-----|------|---------|
| 2026-03-26 | v2.0 | 精简文档，合并重复章节 |
| 2026-03-31 | v3.0 | 全面升级为iOS风格：毛玻璃Tab栏、SVG线性图标、大圆角、呼吸感留白、字体层级优化 |
| 2026-04-07 | v3.1 | 新增顶部状态栏和导航栏设计规范，新增绿色到青色渐变体系，新增内容卡片样式规范 |
| 2026-04-22 | v3.2 | 新增管理后台（Web端）完整设计规范：布局架构、色彩体系、组件规范（顶部栏/侧边栏/面包屑/标签页/搜索区/表格/分页/表单/按钮/树形菜单/左右分栏）、页面类型模板 |
| 2026-06-11 | v3.3 | 新增监管PC端（Web端）完整设计规范：布局架构、色彩规范（品牌绿色系）、顶部导航栏、左侧侧边栏（白色背景+绿色选中）、搜索区域、状态统计卡片、数据表格、分页器、审核弹窗（标题栏/Tab栏/表单/底部操作按钮）、审批记录弹窗（时间线样式）、页面类型模板；附录新增监管PC端色板和尺寸速查 |
| 2026-06-11 | v3.4 | 新增多端原型基线约束：明确施工端移动端、监管端移动端、巡检端移动端、监管端PC端、大屏PC端的原型归属规则；新增移动端三类工程 Tab 强制约束，要求涉及小型工程、住宅装修、零星作业的共用场景默认按三类 Tab 组织 |
